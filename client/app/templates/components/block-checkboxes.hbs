<h2>{{entry.block.label}}</h2>

{{#if entry.block.note}}
  <p class="note">{{sanitize-html entry.block.note}}</p>
{{/if}}

{{#each entry.block.options as |option index|}}
  {{#if option.value}}
    <label>
      {{check-box value=option.value groupValue=entry.value}}
      {{sanitize-html option.label}}
    </label>
  {{else}}
    <label>
      {{check-box value=option groupValue=entry.value}}
      {{sanitize-html option}}
    </label>
  {{/if}}
{{/each}}

{{entry-error-messages entry=entry}}
